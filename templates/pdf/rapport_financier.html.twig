<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Rapport Financier - {{ user.nom }}</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            margin: 0; 
            padding: 20px;
            color: #000; 
            line-height: 1.4;
            background: #fff;
        }
        
        .header { 
            margin-bottom: 30px; 
            border-bottom: 1px solid #000; 
            padding-bottom: 20px; 
        }
        
        .header-top {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }
        
        .logo-section {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #000;
            font-style: italic;
        }
        
        .partnership {
            font-size: 10px;
            color: #666;
            margin-top: 5px;
        }
        
        .page-info {
            text-align: right;
            font-size: 12px;
            color: #666;
        }
        
        .contact-info {
            font-size: 10px;
            color: #666;
            margin-top: 5px;
        }
        
        .header h1 { 
            color: #000; 
            margin: 0; 
            font-size: 20px;
            text-align: center;
            font-weight: bold;
            text-transform: uppercase;
        }
        
        .header h2 { 
            color: #000; 
            margin: 10px 0; 
            font-size: 16px;
            text-align: center;
        }
        
        .header p {
            margin: 5px 0;
            color: #666;
            text-align: center;
        }
        
        .summary { 
            background: #f9f9f9; 
            padding: 20px; 
            border: 1px solid #000; 
            margin-bottom: 30px; 
        }
        
        .summary h3 {
            margin-top: 0;
            color: #000;
            font-weight: bold;
        }
        
        .summary-grid { 
            display: grid; 
            grid-template-columns: repeat(3, 1fr); 
            gap: 20px; 
        }
        
        .summary-item { 
            text-align: center; 
        }
        
        .summary-value { 
            font-size: 24px; 
            font-weight: bold; 
            color: #007bff; 
        }
        
        .summary-label { 
            font-size: 14px; 
            color: #666; 
            margin-top: 5px;
        }
        
        .accounts-section { 
            margin-bottom: 30px; 
        }
        
        .accounts-section h3 {
            color: #007bff;
            border-bottom: 1px solid #ddd;
            padding-bottom: 5px;
            margin-bottom: 15px;
        }
        
        .account-card { 
            border: 1px solid #ddd; 
            border-radius: 5px; 
            padding: 15px; 
            margin-bottom: 15px; 
            background: #f8f9fa;
        }
        
        .account-header { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            margin-bottom: 10px; 
        }
        
        .account-name { 
            font-weight: bold; 
            color: #007bff; 
            font-size: 16px;
        }
        
        .account-balance { 
            font-size: 18px; 
            font-weight: bold; 
            color: #28a745;
        }
        
        .account-balance.negative {
            color: #dc3545;
        }
        
        .account-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 10px;
            margin-top: 10px;
        }
        
        .account-detail {
            font-size: 12px;
        }
        
        .account-detail strong {
            color: #666;
        }
        
        .footer { 
            margin-top: 30px; 
            text-align: center; 
            color: #666; 
            font-size: 12px; 
            border-top: 1px solid #ddd;
            padding-top: 15px;
        }
        
        .no-data {
            text-align: center;
            color: #666;
            font-style: italic;
            padding: 20px;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .stat-card {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            text-align: center;
        }
        
        .stat-value {
            font-size: 20px;
            font-weight: bold;
            color: #007bff;
        }
        
        .stat-label {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>RAPPORT FINANCIER</h1>
        <h2>{{ user.nom }} {{ user.prenoms }}</h2>
        <p>Période du {{ dateDebut|date('d/m/Y') }} au {{ dateFin|date('d/m/Y') }}</p>
    </div>

    <div class="summary">
        <h3>Vue d'ensemble</h3>
        <div class="summary-grid">
            <div class="summary-item">
                <div class="summary-value">{{ stats.nombreComptes }}</div>
                <div class="summary-label">Comptes Actifs</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">{{ stats.nombreMouvements }}</div>
                <div class="summary-label">Mouvements</div>
            </div>
            <div class="summary-item">
                <div class="summary-value">{{ stats.totalPatrimoine|number_format(0, ',', ' ') }} XOF</div>
                <div class="summary-label">Patrimoine Total</div>
            </div>
        </div>
    </div>

    {% if comptes|length > 0 %}
        <div class="accounts-section">
            <h3>Détail des Comptes</h3>
            {% for compte in comptes %}
                <div class="account-card">
                    <div class="account-header">
                        <div class="account-name">{{ compte.nom }}</div>
                        <div class="account-balance {{ compte.soldeActuel < 0 ? 'negative' : '' }}">
                            {{ compte.soldeActuel|number_format(0, ',', ' ') }} {{ compte.devise ? compte.devise.code : 'XOF' }}
                        </div>
                    </div>
                    <div class="account-details">
                        <div class="account-detail">
                            <strong>Type:</strong> {{ compte.typeLabel }}
                        </div>
                        <div class="account-detail">
                            <strong>Devise:</strong> {{ compte.devise ? compte.devise.code : 'XOF' }}
                        </div>
                        <div class="account-detail">
                            <strong>Solde Initial:</strong> {{ compte.soldeInitial|number_format(0, ',', ' ') }} {{ compte.devise ? compte.devise.code : 'XOF' }}
                        </div>
                        <div class="account-detail">
                            <strong>Créé le:</strong> {{ compte.dateCreation|date('d/m/Y') }}
                        </div>
                    </div>
                    {% if compte.description %}
                        <div class="account-detail" style="margin-top: 10px;">
                            <strong>Description:</strong> {{ compte.description }}
                        </div>
                    {% endif %}
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="no-data">
            Aucun compte trouvé.
        </div>
    {% endif %}

    {% if mouvements|length > 0 %}
        <div class="accounts-section">
            <h3>Statistiques des Mouvements</h3>
            <div class="stats-grid">
                {% set entrees = 0 %}
                {% set sorties = 0 %}
                {% for mouvement in mouvements %}
                    {% if mouvement.type in ['entree', 'don'] %}
                        {% set entrees = entrees + mouvement.montant %}
                    {% else %}
                        {% set sorties = sorties + mouvement.montant %}
                    {% endif %}
                {% endfor %}
                
                <div class="stat-card">
                    <div class="stat-value positive">+{{ entrees|number_format(0, ',', ' ') }} XOF</div>
                    <div class="stat-label">Total Entrées</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-value negative">-{{ sorties|number_format(0, ',', ' ') }} XOF</div>
                    <div class="stat-label">Total Sorties</div>
                </div>
            </div>
        </div>
    {% endif %}

    <div class="footer">
        <p>Rapport généré le {{ "now"|date('d/m/Y à H:i') }} pour {{ user.nom }} {{ user.prenoms }}</p>
        <p>SoldeTrack - Gestion de Finances Personnelles</p>
    </div>
</body>
</html>
