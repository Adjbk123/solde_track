<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Relevé de Compte - {{ compte.nom }}</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            margin: 0; 
            padding: 20px;
            color: #000; 
            line-height: 1.4;
            background: #fff;
        }
        
        .header { 
            margin-bottom: 30px; 
            border-bottom: 1px solid #000; 
            padding-bottom: 20px; 
        }
        
        .header-top {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 20px;
        }
        
        .logo-section {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #000;
            font-style: italic;
        }
        
        .partnership {
            font-size: 10px;
            color: #666;
            margin-top: 5px;
        }
        
        .partner-name {
            font-size: 12px;
            color: #000;
            font-weight: bold;
        }
        
        .page-info {
            text-align: right;
            font-size: 12px;
            color: #666;
        }
        
        .contact-info {
            font-size: 10px;
            color: #666;
            margin-top: 5px;
        }
        
        .header h1 { 
            color: #000; 
            margin: 0; 
            font-size: 20px;
            text-align: center;
            font-weight: bold;
            text-transform: uppercase;
        }
        
        .header h2 { 
            color: #000; 
            margin: 10px 0; 
            font-size: 16px;
            text-align: center;
        }
        
        .header p {
            margin: 5px 0;
            color: #666;
            text-align: center;
        }
        
        .info-section { 
            margin-bottom: 20px; 
        }
        
        .info-section h3 { 
            color: #000; 
            border-bottom: 1px solid #000; 
            padding-bottom: 5px; 
            margin-bottom: 15px;
            font-weight: bold;
        }
        
        .info-grid { 
            display: grid; 
            grid-template-columns: 1fr 1fr; 
            gap: 20px; 
            margin-bottom: 20px; 
        }
        
        .info-item { 
            padding: 10px; 
            border: 1px solid #ddd; 
        }
        
        .info-label { 
            font-weight: bold; 
            color: #000; 
            font-size: 12px;
            text-transform: uppercase;
        }
        
        .info-value { 
            color: #000; 
            margin-top: 5px; 
            font-size: 14px;
        }
        
        .summary { 
            background: #f9f9f9; 
            padding: 15px; 
            border: 1px solid #000; 
            margin-bottom: 20px; 
        }
        
        .summary h3 {
            margin-top: 0;
            color: #000;
            font-weight: bold;
        }
        
        .summary-grid { 
            display: grid; 
            grid-template-columns: repeat(4, 1fr); 
            gap: 15px; 
        }
        
        .summary-item { 
            text-align: center; 
        }
        
        .summary-value { 
            font-size: 16px; 
            font-weight: bold; 
            color: #000; 
        }
        
        .summary-label { 
            font-size: 12px; 
            color: #666; 
            margin-top: 5px;
        }
        
        .movements-table { 
            width: 100%; 
            border-collapse: collapse; 
            margin-top: 20px; 
        }
        
        .movements-table th, 
        .movements-table td { 
            border: 1px solid #000; 
            padding: 8px; 
            text-align: left; 
        }
        
        .movements-table th { 
            background: #f0f0f0; 
            color: #000; 
            font-weight: bold;
        }
        
        .movements-table tr:nth-child(even) { 
            background: #f9f9f9; 
        }
        
        .amount { 
            text-align: right; 
            font-weight: bold; 
        }
        
        .amount.positive { 
            color: #000; 
        }
        
        .amount.negative { 
            color: #000; 
        }
        
        .footer { 
            margin-top: 30px; 
            text-align: center; 
            color: #666; 
            font-size: 12px; 
            border-top: 1px solid #000;
            padding-top: 15px;
        }
        
        .no-data {
            text-align: center;
            color: #666;
            font-style: italic;
            padding: 20px;
        }
        
        .account-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
        }
        
        .account-details {
            flex: 1;
        }
        
        .account-details div {
            margin-bottom: 5px;
        }
        
        .account-details strong {
            font-weight: bold;
        }
        
        .balance-info {
            text-align: right;
        }
        
        .balance-label {
            font-size: 12px;
            color: #666;
        }
        
        .balance-value {
            font-size: 16px;
            font-weight: bold;
            color: #000;
        }
        
        .period-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            align-items: center;
        }
        
        .period-dates {
            font-size: 14px;
            color: #000;
        }
        
        .opening-balance {
            text-align: right;
        }
        
        .opening-balance-label {
            font-size: 12px;
            color: #666;
        }
        
        .opening-balance-value {
            font-size: 16px;
            font-weight: bold;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="header-top">
            <div class="logo-section">
                <div>
                    <div class="logo">SoldeTrack</div>
                    <div class="partnership">Gestion de Finances Personnelles</div>
                </div>
            </div>
            <div class="page-info">
                <div>PAGE 1/1</div>
                <div class="contact-info">Adresse et contact de l'organisme</div>
            </div>
        </div>
        <h1>RELEVÉ DE COMPTE</h1>
    </div>

    <div class="account-info">
        <div class="account-details">
            <div><strong>{{ user.nom }} {{ user.prenoms }}</strong></div>
            <div>{{ user.email }}</div>
            <div><strong>Compte:</strong> {{ compte.nom }}</div>
            {% if compte.numero %}
                <div><strong>Numéro:</strong> {{ compte.numero }}</div>
            {% endif %}
        </div>
    </div>
    
    <div class="period-info">
        <div class="period-dates">Du {{ dateDebut|date('d/m/Y') }} au {{ dateFin|date('d/m/Y') }}</div>
        <div class="opening-balance">
            <div class="opening-balance-label">Solde d'ouverture</div>
            <div class="opening-balance-value">{{ soldeInitial|number_format(2, ',', ' ') }}{{ compte.devise ? compte.devise.code : '€' }}</div>
        </div>
    </div>


    {% if mouvements|length > 0 %}
        <table class="movements-table">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Description</th>
                    <th>Crédit ({{ compte.devise ? compte.devise.code : 'XOF' }})</th>
                    <th>Débit ({{ compte.devise ? compte.devise.code : 'XOF' }})</th>
                </tr>
            </thead>
            <tbody>
                {% for mouvement in mouvements %}
                    <tr>
                        <td>{{ mouvement.date|date('d/m/Y') }}</td>
                        <td>{{ mouvement.typeLabel }}</td>
                        <td>{{ mouvement.description ?: 'Transaction SoldeTrack' }}</td>
                        {% if mouvement.type in ['entree', 'don'] %}
                            <td class="amount">{{ mouvement.montant|number_format(2, ',', ' ') }}</td>
                            <td class="amount"></td>
                        {% else %}
                            <td class="amount"></td>
                            <td class="amount">{{ mouvement.montant|number_format(2, ',', ' ') }}</td>
                        {% endif %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <div style="margin-top: 20px;">
            <div style="display: flex; justify-content: space-between; margin-bottom: 10px; font-size: 12px;">
                <span><strong>Total Entrées:</strong></span>
                <span style="text-align: right; width: 200px;">
                    <span style="margin-right: 50px;">{{ totalEntrees|number_format(2, ',', ' ') }}{{ compte.devise ? compte.devise.code : '€' }}</span>
                    <span>-</span>
                </span>
            </div>
            <div style="display: flex; justify-content: space-between; margin-bottom: 10px; font-size: 12px;">
                <span><strong>Total Sorties:</strong></span>
                <span style="text-align: right; width: 200px;">
                    <span style="margin-right: 50px;">-</span>
                    <span>{{ totalSorties|number_format(2, ',', ' ') }}{{ compte.devise ? compte.devise.code : '€' }}</span>
                </span>
            </div>
            <div style="display: flex; justify-content: space-between; background: #f0f0f0; padding: 10px; font-weight: bold; margin-top: 10px;">
                <span><strong>Solde final</strong></span>
                <span><strong>{{ soldeActuel|number_format(2, ',', ' ') }}{{ compte.devise ? compte.devise.code : '€' }}</strong></span>
            </div>
        </div>
    {% else %}
        <div class="no-data">
            Aucun mouvement trouvé pour cette période.
        </div>
    {% endif %}

    <div class="footer">
        <p>© {{ "now"|date('Y') }} SoldeTrack - Gestion de Finances Personnelles</p>
    </div>
</body>
</html>
